name: Deploy NestJS App




on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: SSH & Deploy to VPS
    runs-on: ubuntu-latest

    steps:
      - name: 📡 Connect to VPS and deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd ~/apps/brado

            echo "✅ Pulling latest code..."
            git pull

            echo "stopping containers"
            sudo docker-compose down --remove-orphans
            
            echo "📦 Rebuilding Docker images..."
            sudo docker-compose build

            echo "🔁 Restarting containers..."
            sudo docker-compose up -d
#  push:
#    paths:
#      - 'apps/backend/**'
#      - 'packages/**'
#
#jobs:
#  deploy:
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout code
#        uses: actions/checkout@v3
#
#      - name: Set up Node.js
#        uses: actions/setup-node@v3
#        with:
#          node-version: '18'
#
#      - name: Install dependencies
#        run: npm install
#
#      - name: Build NestJS app
#        run: npx turbo run build --filter=backend
#
#      - name: Upload dist
#        uses: appleboy/scp-action@v0.1.4
#        with:
#          host: ${{ secrets.VPS_HOST }}
#          username: ${{ secrets.VPS_USER }}
#          key: ${{ secrets.VPS_SSH_KEY }}
#          source: "apps/backend/dist"
#          target: "~/backend/dist"
#
#      - name: Upload node_modules
#        uses: appleboy/scp-action@v0.1.4
#        with:
#          host: ${{ secrets.VPS_HOST }}
#          username: ${{ secrets.VPS_USER }}
#          key: ${{ secrets.VPS_SSH_KEY }}
#          source: "apps/backend/node_modules"
#          target: "~/backend/node_modules"
#      - name: Restart backend service
#        uses: appleboy/ssh-action@v1.0.0
#        with:
#          host: ${{ secrets.VPS_HOST }}
#          username: ${{ secrets.VPS_USER }}
#          key: ${{ secrets.VPS_SSH_KEY }}
#          script: |
#            cd ~/backend/apps/backend
#            npm install --production
#            /home/ubuntu/.nvm/versions/node/v22.12.0/bin/pm2 restart backend || /home/ubuntu/.nvm/versions/node/v22.12.0/bin/pm2 start dist/main.js --name backend
